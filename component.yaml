# Action Orchestrator 组件声明（组件 id: action-orchestrator）
id: action-orchestrator
dir:
  - cognition/K-Action_orchestrator
  - action/K-Action_orchestrator
version: 1.0.0
provides:
  - action.reconcile
  - action.operate     # Action Operation 统一编排：create/update/execute/validate（Type Contract 分派）
requires:
  - action.type_registry
  - knowledge.query    # 消费 ActionCandidate / Action Specification v1
