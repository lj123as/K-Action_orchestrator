---
desired_action_type: harness
cognition_ref: cognition/harness/README.md
reason: prove the KA-System Registry -> K-Action_orchestrator -> harness-factory chain
---

# Example ActionIntent (harness)

Same chain, different Action Type: `harness` resolves to harness-factory. The planned harness
action defaults to `runtime provider = agent-runtime` and `execution semantics = agent-loop`,
as declared in harness-factory/component.yaml.

